#include<stdio.h>
#include<math.h>
int* primitive_root(int);

int main(){
	return 0;
}
int* primitive_root(int q){
	int g;
	for(int i=1;i<=q-1;i++){
		g = i;
		int* val_arr = (int*)malloc(sizeof(int)*q);
		for(int j=0;j<q-1;j++){
			
		}
	}
}
